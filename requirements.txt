# ──────────────────────────────────────────────────────────────────────────────
# NeuroScan AI — Dependencies
# Made by Mohammed Mourad Abdelhafidi
#
# NOTE: torch is NOT listed here. Install it separately:
#   CPU:  pip install torch --index-url https://download.pytorch.org/whl/cpu
#   GPU:  pip install torch  (installs CUDA-enabled version)
# ──────────────────────────────────────────────────────────────────────────────

# Core ML & Model Inference
transformers>=4.42.0
safetensors>=0.4.0
huggingface_hub>=0.23.0
tokenizers>=0.19.0

# Image Processing
Pillow>=10.0.0

# Web Framework
streamlit>=1.36.0

# PDF Generation
fpdf2>=2.8.0

# Data Handling (used by download_benchmark.py)
pyarrow>=15.0.0
pandas>=2.0.0
requests>=2.31.0
datasets>=2.19.0

# Utilities
numpy>=1.24.0
